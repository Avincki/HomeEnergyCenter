<!-- AUTO-GENERATED by ClaudeContextGenerator — do not edit manually -->

# CLAUDE.md — hsbGeoLibExt

## Repositories

| Repo | Role | Relative Path | Docs Location |
|---|---|---|---|
| hsbGeoLibExt | Main repository | `.` | `docs/llms/` |
| hsbGeoLib | Sibling DLL repo | `../../../beamapps/Hsb_NetApi/hsbGeoLib` | `CLAUDEDOCS_hsbGeoLib/` (local fallback — sibling is read-only) |

## Build & Platform

- **Source sub-folder:** ``
- **File extensions:** `.h`, `.cs`, `.lsp`

## Personal Context

At session start, read `CLAUDE.alex-vinckier.md` from this repo root, where
`<username>` = `git config user.name` lowercased with spaces replaced by
hyphens. Also read `CLAUDE.<username>.history.md` from the repo root and
from the current working folder if either file exists. At session end,
append a dated entry to the history file in the current working folder,
or to the repo root history file if the work spanned multiple folders.

## Documentation Map

### This repository

- `docs/llms/hsbGeoLibExt/index.md` — hsbGeoLibExt module documentation
- `docs/llms/legacy/legacy/index.md` — legacy/legacy module documentation

### hsbGeoLib (sibling — read-only, docs in local fallback)

- `CLAUDEDOCS_hsbGeoLib/hsbGeoLib/index.md` — hsbGeoLib module documentation

## Path Resolution

`/foo/Bar.h` → `docs/llms/foo/Bar.md`

Sibling `hsbGeoLib` (read-only): `../../../beamapps/Hsb_NetApi/hsbGeoLib//foo/Bar.cs` → `CLAUDEDOCS_hsbGeoLib/foo/Bar.md`

## Legacy

Legacy Lisp routines are archived in `legacy/`. These files are read-only
reference material — do not modify them. Consult them when working on
functions that replace or modernize legacy survey calculations.

## Universal Rules

_(no universal rules configured)_

